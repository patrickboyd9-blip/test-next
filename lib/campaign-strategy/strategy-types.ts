export interface CampaignStrategy {
    audience: string
    offer: string
    mailFormat: string
    cadence: string
    messaging: string[]
    callToAction: string
    successMetrics: string[]
    assumptions: string[]
  }